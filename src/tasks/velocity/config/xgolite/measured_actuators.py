"""Measured XGO-Lite2 actuator constants (GENERATED — do not hand-edit).

Generated by scripts/apply_servo_fit.py on 2026-07-15T18:04:31
from servo_fit_v4_perjoint.json (fit created 2026-07-15T18:03:14, sessions: ['/home/dvv/dev/bots/servo_id_data/servo_id/2026-07-15_14-24-44_midrange']).

Stage-1 servo fit: per-joint MJCF dynamics, PD gains, torque-speed clamp
and the global command delay. See the fit report next to the fit.json for
sim-vs-real validation. Regenerate with:
  PYTHONPATH=. .venv/bin/python scripts/apply_servo_fit.py servo_fit_v4_perjoint.json \
    --write-cfg src/tasks/velocity/config/xgolite/measured_actuators.py
"""

# joint -> dict(damping, armature, frictionloss, kp, kd,
#               qd_knee, qd_max, deadband)
# qd_knee/qd_max parameterize the fitted piecewise torque-speed curve
# (driving torque flat at TAU_MAX up to qd_knee, linear taper to zero at
# qd_max) consumed by the knee-form TorqueSpeedClamp.
# deadband (fitted lost-motion nominal, rad) is carried for reference: the
# v19 preset randomizes the deadband WIDE instead of pinning these — the
# measured small-amplitude delivery of 0.79-0.93 implies up to ~0.02 rad
# effective lost motion (dynamic hysteresis the model cannot express).
MEASURED_JOINTS = {
  'fl_hip': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0117},
  'fl_thigh': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0121},
  'fl_calf': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0142},
  'fr_hip': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0157},
  'fr_thigh': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0123},
  'fr_calf': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0114},
  'bl_hip': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0091},
  'bl_thigh': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.013},
  'bl_calf': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0126},
  'br_hip': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0149},
  'br_thigh': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0146},
  'br_calf': {'damping': 0.004086, 'armature': 0.000563, 'frictionloss': 0.017235, 'kp': 5.5912, 'kd': 0.0738, 'qd_knee': 3.65, 'qd_max': 10.4638, 'deadband': 0.0108},
}

TAU_MAX = 0.22  # N*m, pinned fit anchor (== XML forcerange)

# Class means (for the scalar knee-form TorqueSpeedClamp API).
MEASURED_QD_KNEE = 3.65
MEASURED_QD_MAX = 10.4638

# Global command delay (firmware receipt -> actuation) and the
# recommended per-servo delay DR range (measured onset spread +/-22 ms),
# in physics steps at 2.0 ms.
MEASURED_DELAY_MS = 40.98
DELAY_MIN_LAG_STEPS = 9
DELAY_MAX_LAG_STEPS = 31


def apply_measured_joint_dynamics(spec) -> None:
  """Write fitted MJCF joint fields + PD gains into an MjSpec in place.

  Call on the spec returned by ``xgolite_constants.get_spec()`` before
  compilation. Position actuator convention: gainprm[0] = kp,
  biasprm = (0, -kp, -kd).
  """
  for joint, p in MEASURED_JOINTS.items():
    j = spec.joint(joint + "_joint")
    j.damping = p["damping"]
    j.armature = p["armature"]
    j.frictionloss = p["frictionloss"]
    a = spec.actuator(joint + "_joint")
    a.gainprm[0] = p["kp"]
    a.biasprm[1] = -p["kp"]
    a.biasprm[2] = -p["kd"]


def enable_measured_torque_speed_clamp(cfg) -> None:
  """Wire the knee-form clamp with measured class-mean parameters.

  The clamp API is scalar (one tau_max/qd_knee/qd_max for all joints);
  per-joint values live in MEASURED_JOINTS for a future per-joint clamp.
  """
  from src.tasks.velocity.config.xgolite.sim_fidelity import (
    enable_torque_speed_clamp,
  )

  enable_torque_speed_clamp(
    cfg,
    tau_max=TAU_MAX,
    qd_knee=MEASURED_QD_KNEE,
    qd_max=MEASURED_QD_MAX,
  )


def measured_delay_actuator_cfg():
  """DelayedActuatorCfg with the measured delay DR range."""
  import dataclasses

  from src.assets.robots.xgolite.xgolite_constants import (
    XGOLITE_XML_ACTUATOR,
  )

  return dataclasses.replace(
    XGOLITE_XML_ACTUATOR,
    delay_min_lag=DELAY_MIN_LAG_STEPS,
    delay_max_lag=DELAY_MAX_LAG_STEPS,
  )
